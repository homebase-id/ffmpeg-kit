name: android build

on:
  workflow_dispatch:
  push:
    tags: 
      - '*'

jobs:
  build-android-main-on-linux:
    name: android main on linux
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        # NDK r27+ defaults to 16KB ELF page-size alignment, required by
        # Android 15+ on 16KB-page devices. See U12 in CUSTOMIZATION.md.
        ndk-version: [ 'r27d-linux' ]
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v6
      - name: set up adopt jdk 17
        uses: actions/setup-java@v5
        with:
          distribution: 'adopt'
          java-version: '17'
      - name: prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install autoconf automake libtool pkg-config curl git doxygen nasm cmake gcc gperf texinfo yasm bison autogen wget autopoint meson ninja-build ragel groff gtk-doc-tools libtasn1-bin
      - name: prerequisites 2
        run: ${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager --uninstall "cmake;3.10.2.4988404" "cmake;3.18.1"
      - name: upgrade meson
        run: pip install meson --upgrade
      - name: set up android ndk
        run: |
          curl -s "https://dl.google.com/android/repository/android-ndk-${{ matrix.ndk-version }}.zip" -o ndk.zip
          unzip -q -o ndk.zip -d .ndk
          NDK_ROOT="$PWD/.ndk/$(ls .ndk)"
          echo "ANDROID_NDK_ROOT=$NDK_ROOT" >> $GITHUB_ENV
          # U18: NDK r27d ships a host x86_64 libxml2.so in its toolchain
          # lib dir. FFmpeg's libxml2 link probe finds the host copy ahead
          # of our cross-compiled one and fails with "incompatible with
          # armelf_linux_eabi". Remove it — clang doesn't need it for
          # cross-compile.
          rm -f "$NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64/lib/libxml2.so" \
                "$NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64/lib/libxml2.so."*
      - name: run the build script
        # --disable-lib-libaom: clang 18 (NDK r27d) rejects libaom 3.6.1's
        # missing forward declarations. chat-kmp doesn't use AV1 encoding.
        # See U13 in CUSTOMIZATION.md.
        # --disable-lib-sdl: clang 18 rejects SDL 2.0.8's OpenGLES2
        # incompatible function pointer types. chat-kmp doesn't use SDL.
        # See U14.
        # --disable-lib-libvpx: libvpx 1.13.0's configure assumes gcc
        # toolchain paths that NDK r27d removed. chat-kmp doesn't encode
        # VP9 (only decodes, which uses FFmpeg's native vp9 decoder).
        # See U17.
        # Note: libxml2 (U18) is handled at the NDK setup step above by
        # removing the host libxml2.so that ships with NDK r27d. The
        # cross-compiled libxml2 builds normally.
        #
        # B3 Step 1 (size trim): disable the clearly-unused external libs.
        # chat-kmp only uses libx264 (SW H.264 encode) + native built-in
        # decoders/muxers + h264_mediacodec (HW, a configure flag not a
        # lib). Everything below is dead weight for chat-kmp's pipeline.
        #
        # gnutls + gmp pulled forward from Step 2: the full-feature build
        # hangs 4h+ on gnutls's configure/self-test on the CI runner.
        # chat-kmp feeds ffmpeg local files only (no https:// inputs;
        # HLS AES-128 uses ffmpeg's built-in crypto + a local key file),
        # so TLS isn't needed. openh264 is redundant — we encode H.264
        # via libx264 and decode via FFmpeg's native h264 decoder.
        #
        # Conservative-keep set still under test in Step 2: x264, libxml2,
        # zimg, libiconv, gif/jpeg. See CUSTOMIZATION.md B3.
        run: ./android.sh --full --enable-gpl --disable-arm-v7a --disable-lib-srt --disable-lib-libaom --disable-lib-sdl --disable-lib-libvpx --disable-lib-tesseract --disable-lib-libass --disable-lib-fontconfig --disable-lib-freetype --disable-lib-fribidi --disable-lib-rubberband --disable-lib-soxr --disable-lib-chromaprint --disable-lib-libvidstab --disable-lib-lame --disable-lib-shine --disable-lib-twolame --disable-lib-vo-amrwbenc --disable-lib-opencore-amr --disable-lib-libilbc --disable-lib-speex --disable-lib-opus --disable-lib-libvorbis --disable-lib-libtheora --disable-lib-libwebp --disable-lib-kvazaar --disable-lib-x265 --disable-lib-xvidcore --disable-lib-dav1d --disable-lib-snappy --disable-lib-gnutls --disable-lib-gmp --disable-lib-openh264
      - name: Upload AAR as workflow artifact
        uses: actions/upload-artifact@v7
        if: ${{ always() }}
        with:
          name: ffmpeg-kit-aar
          path: prebuilt/bundle-android-aar/ffmpeg-kit/ffmpeg-kit.aar
          if-no-files-found: warn
          retention-days: 30
      - name: Upload Release Asset (.integrity)
        id: upload-release-asset-integrity
        uses: ncipollo/release-action@v1
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          allowUpdates: true
          artifacts: "prebuilt/bundle-android-aar/ffmpeg-kit/ffmpeg-kit.aar"
          omitBodyDuringUpdate: true
          omitNameDuringUpdate: true
          prerelease: true
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: print build logs
        if: ${{ always() }}
        run: cat build.log
      - name: print ffbuild logs
        if: ${{ failure() }}
        run: '[[ -f ./src/ffmpeg/ffbuild/config.log ]] && tail -50 ./src/ffmpeg/ffbuild/config.log'